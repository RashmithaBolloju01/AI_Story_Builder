# AI Story Builder (Hybrid)

This project provides a simple story engine that builds a structured story and optionally expands it using a local transformer model.

## Setup

1. Create venv and activate:
   ```bash
   python -m venv venv
   source venv/bin/activate
